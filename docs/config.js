// Ajuste aqui para apontar para sua API em produção
// Exemplo: window.CONFIG = { API_URL: 'https://financeiro.avilatransportes.com.br' };
window.CONFIG = window.CONFIG || {
  API_URL: '' // Defina a URL do backend aqui se front estiver no GitHub Pages
};
